쿠키를 이용하여 구현하고자 하는 기능

rememberme 를 체크하고 로그인 했을 떄 userid 와 rememberme 라는 이름으로 쿠키를 저장하여
다시 login. jsp 에 접속 했을 떄 사용자 id가 마지막 접속시 아이디로 값이 채워지고 

rememberme 체크박스가 체크가 되어있도록 설정 

필요한 것 
$(function(){
	출력 이벤트를 감지한다. ( sign in 버튼 클릭)  
	쿠키 설정
	submit; 
})
